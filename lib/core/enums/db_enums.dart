enum NoteType { note, password }

enum NoteCategory {
  personal,
  work,
  finance,
  social,
  development,
  entertainment,
  travel,
  health,
}

enum PasswordStrength { weak, fair, strong, veryStrong }

enum ItemTag {
  urgent,
  important,
  archived,
  shared,
  encrypted,
  temporary,
  pinned,
}

enum SortBy { nameAsc, nameDesc, dateUpdated, dateCreated, category }
