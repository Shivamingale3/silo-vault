enum AppKeys {
  appPin,
  accessToken,
  refreshToken,
  biometric,
  maxUnlockAttempts,
  lockoutTill,
  failedAttempts,
  autoLockTimeout,
  encryptionKey,
  appTheme,
  syncEnabled,
  syncPassword,
  wrappedEncryptionKey,
  syncSalt,
  cachedDisplayName,
  cachedEmail,
  cachedPhotoPath,
  updateAvailable,
  updateVersion,
}

enum AppThemeMode { light, dark, amoled }
