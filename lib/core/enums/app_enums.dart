enum AppKeys {
  appPin,
  accessToken,
  refreshToken,
  biometric,
  maxUnlockAttempts,
  lockoutTill,
  failedAttempts,
  autoLockTimeout,
  encryptionKey,
  appTheme,
}

enum AppThemeMode { light, dark, amoled }
