enum AppKeys {
  appPin,
  accessToken,
  refreshToken,
  biometric,
  maxUnlockAttempts,
  lockoutTill,
  failedAttempts,
  autoLockTimeout,
  encryptionKey,
}

enum AppThemeMode { light, dark, amoled }
